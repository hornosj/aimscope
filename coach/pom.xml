<?xml version="1.0" encoding="UTF-8"?>
<!-- Casca Embabel do coach (espelho do beautiful-linkedin, enxuta p/ batch).
     O deps.edn continua sendo o runtime do coach determinístico
     (ingest/diagnose/plan/outcome); ESTE pom só existe p/ o agente de
     narrativa (Spring + AgentPlatform + OpenRouter).
     Build:  mvn -q -DskipTests package  ->  target/aimscope-coach.jar
     Run:    java -jar target/aimscope-coach.jar  (exige OPENROUTER_APIKEY p/ LLM;
             sem chave, o GOAP cai no fallback determinístico e ainda entrega) -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>aimscope</groupId>
  <artifactId>aimscope-coach</artifactId>
  <version>0.1.0</version>
  <packaging>jar</packaging>
  <description>Agente Embabel (GOAP+LLM) de narrativa do aimscope</description>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.4.1</version>
    <relativePath/>
  </parent>

  <properties>
    <java.version>21</java.version>
    <embabel.version>0.4.0</embabel.version>
    <clojure.version>1.12.0</clojure.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Jackson uniforme ANTES da BOM embabel (primeira declaração vence) -->
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>2.21.2</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.embabel.agent</groupId>
        <artifactId>embabel-agent-dependencies</artifactId>
        <version>${embabel.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.embabel.agent</groupId>
      <artifactId>embabel-agent-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>com.embabel.agent</groupId>
      <artifactId>embabel-agent-starter-openai</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.clojure</groupId>
      <artifactId>clojure</artifactId>
      <version>${clojure.version}</version>
    </dependency>
    <dependency>
      <groupId>cheshire</groupId>
      <artifactId>cheshire</artifactId>
      <version>5.13.0</version>
    </dependency>
  </dependencies>

  <repositories>
    <repository>
      <id>spring-milestones</id>
      <url>https://repo.spring.io/milestone</url>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
    <repository>
      <id>clojars</id>
      <url>https://repo.clojars.org/</url>
    </repository>
  </repositories>

  <build>
    <finalName>aimscope-coach</finalName>
    <sourceDirectory>src-java</sourceDirectory>
    <resources>
      <!-- application.properties + models/openai-models.yml -->
      <resource><directory>resources</directory></resource>
      <!-- .clj CRUS -> BOOT-INF/classes (NO-AOT); require em runtime -->
      <resource><directory>src</directory></resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>aimscope.boot.App</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
